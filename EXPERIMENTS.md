# EXPERIMENTS.md

## 2026-06-18 random_warmup

| date | cost_structure | action_range | reward_version | centering | note | change |
|------|----------------|--------------|----------------|-----------|------|--------|
| 2026-07-06 | b_rl | a[-1,1]_b[0,1] | J | False | tat_dense | **step_reward = -rail_tat_penalty + w_tat_dense * g (w=0.3).** reward_version I(rail_tat_only)에서 actor gradient vanishing(grad/actor_norm 0.007~0.015) → NaN explosion 발생(run r1ye9c49, step 216300). 원인: sparse reward로 Q surface flat, critic loss 0.01~0.02로 overfit → dQ/da≈0 → actor gradient 소멸. 에피소드 말기 step_reward 48% zero spike가 NaN 트리거. 수정: get_step_reward에 `+ w_tat_dense * self._last_g` 추가. `self._last_g`는 get_global_reward의 정규화된 결과(매 스텝 dense, 레일 루프 전에 이미 계산됨). w_tat_dense=0이면 reward_version I와 동일(backward compat). ClientAlgorithm.py에만 수정하면 ClientAlgorithm_region.py 상속으로 자동 반영. reward_weights에 `tat_dense: 0.3` 추가. |
| 2026-07-04 | b_rl | a[-1,1]_b[0,1] | I | False | railtat_only | Fixed the region harness compatibility bug: `_compute_rail_rewards()` no longer passes the obsolete `g` argument to the reward-I `get_step_reward(rail, pclient)` API. Reward formula, observations, actions, replay, and metrics are unchanged. |
| 2026-07-04 | b_rl | a[-1,1]_b[0,1] | I | False | railtat_only | **step_reward = -rail_tat_credit 단독 사용.** global(marginal TAT EMA + Δop + backlog)과 local(oht_count/predicted/stop/idle/capacity) 항 완전 제거. `get_step_reward` 반환값을 `-self._rail_tat_penalty(rail)` 하나로 단순화. global/local은 로깅만 유지(비교용). 근거: rail_tat_credit은 이미 무차원(`rel_excess × t/total_time`), 정규화 불필요. event_count ~780/step으로 dense. ozl3xyft 분석에서 rail_tat_mean이 유일하게 단조 감소하던 지표. global reward의 backlog 항은 autocorr=0.69, 학습 신호 기여 사실상 0. op_rate은 낮을수록 효율적이므로 제거해도 무방. `get_step_reward` 시그니처에서 `g` 파라미터 제거, 호출부도 동일하게 수정. |
| 2026-07-04 | b_rl | a[-1,1]_b[0,1] | H | False | scale10 | **scale_end 0.4→1.0.** `scalepen` (`1xpcc8ob`) 결과: pretanh_abs_mean 17→0.35, eval_std 5.96e-8→0.186 — actor collapse 해결. 분석: scale fix가 주역(step 10100 시점 이미 pretanh=0.014, penalty는 그 이전에 필요 없었음), penalty는 0.11% 기여로 non-binding. 이전 실험(11ep 분석)에서 scale_end=1.0이 불안정했던 원인은 clip/unclip mismatch였음 — scale fix가 그 원인을 제거했으므로 scale_end=1.0 재시도 안전. `ClientAlgorithm_region.train_config()`에서 `config["curriculum"]["scale_end"] = 1.0` override (ClientAlgorithm.py per-rail path는 건드리지 않음). pretanh_penalty_coef=0.1 유지: 정상 범위(pretanh≤3)에선 비율 3% 미만으로 non-binding이나, pretanh이 5 이상으로 성장 시 자동 브레이크로 기능. 관측 지표: curriculum/action_scale이 1.0까지 올라가는지, pretanh가 그 과정에서 3 이하를 유지하는지, eval_std 안정적으로 0.1+ 유지하는지. |
| 2026-07-03 | b_rl | a[-1,1]_b[0,1] | H | False | scalepen | **Actor fix v5 — curriculum scale fix + pre-tanh penalty.** Three rounds of architecture fixes (AvgL1Norm, weight_norm, LayerNorm) all failed via whack-a-mole: bounding one variable caused optimizer to compensate through another free variable. Root cause identified as NOT architecture: clip/unclip mismatch in actor loss. `_select_region_actions` applies `* curriculum_scale` (line 329) before storing to buffer, so buffer holds actions in [0, 0.4]. But `learner.py` actor loss called `policy_action()` which returns raw tanh output in [0, 1.0] — 2.5x gap. Critic trained on [0, 0.4] extrapolates into [0.4, 1.0] and gives spurious positive Q gradient → actor drives pre-tanh → +∞. Fix 1 (mismatch removal): `learner.set_curriculum_scale(scale)` called each step from ClientAlgorithm_region before `learn()`; actor loss now computes `pretanh` directly then `new_action = max_action * tanh(pretanh) * mask * curriculum_scale` — critic receives same distribution it was trained on, spurious extrapolation gradient eliminated. Fix 2 (saturation escape): pre-tanh already at 17 → tanh'(17)≈0 → scale fix alone gives zero gradient through tanh → cannot escape. Added `pretanh_penalty = pretanh_penalty_coef * pretanh.pow(2).mean()` to actor loss; gradient of penalty w.r.t. mlp params flows directly (no tanh), magnitude 2*17*coef at current saturation, drives pretanh toward 0 regardless of tanh gradient. `pretanh_penalty_coef=0.1` (config key `algorithm.pretanh_penalty_coef`). Architecture reverted to baseline 5-layer net (no LayerNorm, no weight_norm, no AvgL1Norm). New wandb key: `loss/actor_pretanh_penalty` (should be ~29 at step 0 with pretanh≈17, → ~0.1 when pretanh≈1). Check: `pretanh_abs_mean` ≤ 3, `eval_std` off floor, `loss/actor_pretanh_penalty` declining. |
| 2026-07-02 | b_rl | a[-1,1]_b[0,1] | H | False | actorlnorm | **Actor fix v4 — LayerNorm before fc2 (candidate c).** `j008sotw` (actorwtnorm) confirmed Case C: `fc2_weight_absmean=0.030` stable (weight_norm working), but `pretanh_abs_mean=7.3→17.1` and `scale_relu1_abs_mean=0.44→0.84` — upstream doubled its L2 output to bypass the now-fixed fc2 weight magnitude. Root cause of bypass: with `||fc2_weight||_2=1` fixed, optimizer grew `||h||_2` (fc2 input L2) instead, since Cauchy-Schwarz bound is `||h||_2 * ||fc2_weight||_2 = ||h||_2` and that was the only free variable. Fix: `nn.LayerNorm(hdim)` inserted between relu1 and fc2 in `RegionPolicyHead.net` (net[4]), making fc2 the new net[5]. LayerNorm forces each 256-dim hidden vector to mean=0, std=1 per element, so `||h_LN||_2 = sqrt(256) = 16` regardless of what upstream produces — `||h||_2` inflation path is structurally blocked. Implementation: removed F.normalize weight_norm from `_pre_tanh` (now just `self.net(X)`), removed F import (no longer used). `_layer_scale_probe` updated to 6 stages: `fc0/relu0/fc1/relu1/ln/fc2` — `scale_ln_abs_mean` (≈sqrt(2/π)≈0.80 for unit-normal inputs, fixed) is the new bounded-fc2-input diagnostic. `export_wandb_run.py` KEYS: added `actor/scale_ln_abs_mean`. Check: `scale_ln_abs_mean` should stay ~0.8 (not grow), `fc2_weight_absmean` may now grow freely (watch for Case C'), `pretanh_abs_mean` ≤ 3, `eval_std` off floor. `EXP_META.note` → `actorlnorm`. |
| 2026-07-01 | b_rl | a[-1,1]_b[0,1] | H | False | actorwtnorm | **Actor fix v3 — weight normalization on fc2 (candidate b).** Three-way empirical comparison plan: (a) constant divisor `/sqrt(hdim)`, (b) fc2 weight normalization, (c) LayerNorm before fc2. Running (b) first — most theoretically constrained. The `/sqrt(hdim)` approach (actorfanin, candidate a) was correctly rejected: same mechanism by which upstream learns to produce coherent activations applies to fc2.weight growing to compensate — "slow" but not prevented, especially over 108k+ steps. The real constraint needed is on the MAGNITUDE of fc2's effective weight, making it impossible (not merely slow) for the optimizer to compensate by growing weights. Implementation: `_pre_tanh` now applies `F.linear(h, F.normalize(fc2.weight, dim=1), fc2.bias)` — fc2.weight is normalized to unit L2 norm per row at every forward pass (direction is learned, magnitude is structurally fixed at 1). Sanity check: after multiplying fc2.weight by 100x, pre_tanh output is unchanged (0.2537 both before and after the 100x scaling) — hard-bound confirmed. Initial pre_tanh abs_mean = 0.2537 (vs 8.43 at step 10100 for mwzrsy03/AvgL1Norm). `net` reverted to 5 layers (no AvgL1Norm), no `/sqrt(hdim)`. `_layer_scale_probe` updated to use the same F.normalize path for fc2. `EXP_META.note` → `actorwtnorm`. Target: 30k steps, check `pretanh_abs_mean` and `eval_std`. |
| 2026-07-01 | b_rl | a[-1,1]_b[0,1] | H | False | actorfanin | **Actor fix v2 — fan-in scaling.** `actorl1norm` run (`mwzrsy03`, step 108100) confirmed AvgL1Norm failed: `pretanh_abs_mean=8.43` from step 10100 (first diagnostic log), growing to 13.73 by end, `eval_std=5.96e-8` unchanged. Root cause of the failure: `fc2_weight_absmean` barely moved (0.030→0.032), but pre-tanh grew because upstream layers (fc0/fc1/encoder) learned to produce more coherently-aligned activations with fc2's weight vector — AvgL1Norm actually gave upstream a clean fixed-scale optimization target. L1-mean normalization bounds the L1 mean but allows sparse concentration, so the 256-dim dot-product can still reach `256*1.0*0.030=7.68` (fully coherent case = exactly what was observed at step 10100). LayerNorm has the same problem (Cauchy-Schwarz gives max `||w||_2 * sqrt(256) ≈ 0.51*16=8.2`). **The only fix that can't be learned around is a constant divisor on the pre-tanh OUTPUT**: `return self.net(X) / (self.hdim ** 0.5)`. Even with upstream fully coherent: max pre-tanh = 7.68/16 = 0.48, which keeps tanh in its linear region (`tanh'(0.48)≈0.77`). The divisor is constant (not learned), so fc2 gradient is also scaled by `1/sqrt(hdim)`, structurally slowing re-saturation. Implementation: removed `AvgL1Norm()` layer from `net`, added `self.hdim = hdim` to `__init__`, changed `_pre_tanh` to `return self.net(X) / (self.hdim ** 0.5)`. `_layer_scale_probe` updated to 5 stages again (no norm stage) + new `actor/scale_pretanh_scaled_abs_mean` key logging the post-division magnitude. Random-init sanity: pre-tanh abs_mean=0.0095 (vs 8.43 at first training log in `mwzrsy03`). `export_wandb_run.py` KEYS updated (`scale_norm_abs_mean` removed, `scale_pretanh_scaled_abs_mean` added). `EXP_META.note` → `actorfanin`. |
| 2026-06-30 | b_rl | a[-1,1]_b[0,1] | H | False | actorl1norm | **Actor fix, single variable.** The layer-scale probe (previous entry) on run `zwyu5on1`, already at step 20500 (curriculum scale only 0.10, well before its 0.4 cap), pinpointed the exact amplification site: `actor/scale_relu1_abs_mean=0.878 -> actor/scale_fc2_abs_mean=13.56`, a ~15x jump concentrated entirely in the last `RegionPolicyHead.net` Linear (`net[4]`, `hdim(256)->action_dim`). `actor/fc2_bias_absmean=0.0245` ruled out a bias-offset cause (3 orders of magnitude too small to explain 13.56); `actor/fc2_weight_absmean=0.0356` (an ordinary per-element magnitude) combined with the ~27x gap between the actual output and the random-cancellation estimate (`sqrt(256)*0.878*0.0356≈0.5`) pointed to coherent (non-cancelling) summation across the unnormalized 256-wide hidden layer, consistent with `pretanh_contrib_zs` dominance found in the earlier PHASE-1 probe (the region-uniform `zs` input pushes every rail token's hidden state in the same direction, so the final Linear sums that shared direction over 256 dims with no normalization in between, while the per-rail `H` contribution gets buried). Fix: added an `AvgL1Norm` module (`x / x.abs().mean(-1).clamp(min=eps)`, identical technique already used for `zs` in `RegionEncoder.zs_proj`) directly into `RegionPolicyHead.net` between `relu1` and the final Linear — `net` is now `Linear(384,256)->ReLU->Linear(256,256)->ReLU->AvgL1Norm->Linear(256,1)`. This is the only change (one inserted layer, no other architecture/reward/buffer/loss change). `_layer_scale_probe`'s `stage_names` updated to include the new `norm` stage (`actor/scale_norm_abs_mean` added to `export_wandb_run.py` KEYS, expected to read ~1.0 by construction). Random-init sanity check (`scratchpad/sanity_avgl1norm.py`) confirms the structural effect: pre-tanh `abs_mean` dropped from the prior ~8-14 regime to ~0.40 even before any training. `EXP_META.note` changed to `actorl1norm` to mark this run distinctly in W&B. Pass condition (user-specified, exactly two metrics, no further probes): `actor/pretanh_abs_mean` should fall to roughly ±3, and `action/eval_std` should move measurably off the `~1e-7` floor. If both hold, this unblocks the originally-planned reward/credit-assignment experiments (PHASE 2/3), since the actor will no longer be saturating regardless of what reward signal it receives. |
| 2026-06-30 | b_rl | a[-1,1]_b[0,1] | H | False | alpha05diag | Diagnostics-only follow-up to the PHASE 1 entry below, before deciding PHASE 3. PHASE 1's `actor/pretanh_*` probe on run `x4a40iyk` already showed `pretanh_abs_mean` 8~14 (deep tanh saturation) from the very first post-warmup log (step 10100, curriculum scale still 0.05 — i.e. saturation predates the curriculum freeze at step 40000, ruling out the curriculum schedule as the trigger) and `pretanh_std` only 0.01~0.06 against that 8~14 base, i.e. all rails in a region collapse to nearly the same large pre-tanh value. To localize *where inside* `RegionPolicyHead.net` (`Linear(2*embed_dim+zs_dim,hdim)->ReLU->Linear(hdim,hdim)->ReLU->Linear(hdim,action_dim)`) this blow-up happens — since `pretanh_contrib_H+C+zs` (first-Linear contribution, typically <1.5) is far smaller than the final `pretanh_abs_mean` (8~14), so the amplification must happen in the later layers, not the input block — added two new permanent probes. (1) `RegionPolicyHead._layer_scale_probe`/`RegionTD7Actor.layer_scale_probe_action`/`TokenTD7Learner.probe_layer_scale`: runs the same `views[0]` single-region forward pass and records `abs_mean` after each of the 5 `net` stages (`fc0, relu0, fc1, relu1, fc2`=pre-tanh) as `actor/scale_{stage}_abs_mean`, plus the raw concatenated input's `actor/scale_in_abs_mean` — pinpoints which Linear is responsible for the jump from ~1.5 to ~8-14. (2) `RegionPolicyHead.bias_norms`/`RegionTD7Actor.policy_head_bias_norms`/`TokenTD7Learner.probe_bias_norms`: weight-only (no forward pass needed), reports `actor/{fc0,fc1,fc2}_bias_absmean` and `actor/{fc0,fc1,fc2}_weight_absmean` for all three Linears — tests directly whether the near-constant large pre-tanh value (std 0.01-0.06 on a base of 8-14) comes from a large `fc2.bias` (a learned constant offset baked into the last layer, independent of any rail/region input) versus the weights. Both probes are called from `_log_region_diagnostics` (`ClientAlgorithm_region.py`) at the same 100-step/`views[0]`-only cadence as the existing `pretanh_probe`/`weight_probe`. No reward formula, replay buffer, loss, or action-selection changes — pure diagnostic logging, so `reward_version` stays `H`. Added the 12 new keys (`actor/scale_*`×6, `actor/{fc0,fc1,fc2}_{bias,weight}_absmean`×6) to `export_wandb_run.py` KEYS. |
| 2026-06-30 | b_rl | a[-1,1]_b[0,1] | H | False | alpha05diag | **Credit-assignment improvement PHASE 1/3** (phased plan: measure rail-level reward variance before committing to a token-level critic rewrite). (1) Changed `reward_alpha` default `0.7 → 0.5` in `ClientAlgorithm.py` (config dict + both `.get("reward_alpha", ...)` fallbacks, plus the region-path diagnostic fallback in `ClientAlgorithm_region.py`) — this is the only formula-affecting change in this phase: `get_step_reward`'s `alpha*g + (1-alpha)*l - w*rail_tat` now weights local 50/50 instead of 70/30 global-dominant, in line with the "region averaging collapses the only rail-varying signal" diagnosis from the prior session. (2) Added region-internal reward-variance diagnostics to see whether token-level credit assignment (PHASE 3) would even have a signal to exploit: `_compute_rail_rewards` (`ClientAlgorithm_region.py`) now also captures each rail's normalized local reward `l_i` into `self._last_local_rewards` (previously only the combined per-rail `reward` was kept, and `_last_local_norm` held just the last rail in the loop, same overwrite pattern fixed for `rail_tat` previously). `_push_region_transitions` now computes, per region view, `std(rail_rewards[reward_idxs])` and `std(local_rewards[reward_idxs])`, then logs the mean/max over all regions as `reward/region_internal_std_mean`, `reward/region_internal_std_max` (combined step reward) and `reward/region_local_std_mean` (local-only, g excluded — the purest signal of how much rail-level differentiation exists inside a region before mean-collapse). Bumped `reward_version` G→H (alpha is a reward-formula parameter) and set `EXP_META.note="alpha05diag"`. Replay buffer and loss are untouched in this phase — reward is still pushed as `mean(rail_rewards[reward_idxs])` per region; PHASE 2 will vectorize the replay reward (regression test against this phase's curves) and PHASE 3 (gated on this phase's `reward/region_local_std_mean` being meaningfully large) will move the critic to per-token TD targets. Added the three new keys to `export_wandb_run.py` KEYS. No action/observation/checkpoint format changes. |
| 2026-06-30 | b_rl | 0.5-1.5 | G | False | nextmaskfix | Fixed a region-replay bug where the bootstrap target's mask was wrong: `RegionReplayBuffer.push()` (`replay_buffer.py`) never accepted or stored a `next_mask`, so `sample()` reused the current-step `mask` for `next_mask` (`next_mask[b, :n] = item["mask"]`). `learner.py` already actively consumes `batch["next_mask"]` for the target Q bootstrap (`target_actor.fixed_encoder.token_state(next_states, next_context, next_mask)`, target policy action, target `zsa`, `q_next`), so this was not just a diagnostic gap — the TD target was computed with the wrong region membership mask whenever a region's valid-token count changed between the current and next step. Added a `next_mask` parameter to `push()` (stored as `"next_mask"` in the storage dict, validated against `next_rail_feat`'s length independently of `mask`/`rail_feat`, since region membership can differ between the current and next view of the same region). `ClientAlgorithm_region.py`'s `_push_region_transitions()` now passes `next_view["mask"]` (already available, built by `_build_region_views`) as the new argument. In `sample()`, the collate loop now uses a separate `n_next = next_rail_feat.shape[0]` to slice `next_rail`/`next_mask` (instead of reusing the current view's `n`), and `lmax` is computed over both current and next lengths so a growing region's next view is never truncated. Added `region_mask/next_valid_tokens_mean` to `last_sample_info`/W&B. Checked: replay-buffer contents are never serialized in checkpoints (`buffer_saved` is hardcoded `False` in `_save_checkpoint`, and `resume_checkpoint()` always starts with an empty buffer), so no legacy-entry fallback for missing `next_mask` is needed. Reward formula, action range, observation shape, and checkpoint format are unchanged. Updated `export_wandb_run.py` for the new key. |
| 2026-06-30 | b_rl | 0.5-1.5 | G | False | gradclip | Run `8fa25z29` NaN'd out around step 386600-387200: `grad/encoder_norm` jumped from a normal ~0.01-0.3 to 1.58e10, then `inf`, then 6.57e14 within 3 logging intervals (300 env steps), while `loss/encoder` itself stayed in its normal ~0.001-0.004 range — a classic backward-pass singularity, not a loss blowup. Prime suspect is `avg_l1_norm`'s `x / x.abs().mean(-1).clamp(min=1e-8)` in `networks.py`: when the pre-normalized `zs_proj` output's mean-abs collapses near the `1e-8` floor, the division's backward term (`~1/denom^2`) explodes. Once `grad/encoder_norm` hit `inf`, encoder weights were corrupted and `numeric/eval_action_finite_ratio` dropped from 1.0 to 0.0 within ~300 steps (actions/critic/everything downstream went NaN; the post-crash `action/eval_std=0.0000` plateau is dead NaN-sanitized-to-zero logging, not convergence). Separately, this run's new `actor/w0_absmean_{H,C,zs}` weight-block-norm diagnostic (added in the previous session to test "zs weight block grew abnormally large, causing pre-tanh saturation") came back **negative**: `absmean_zs` (~0.090-0.095) was consistently *smaller* than `absmean_H` (~0.130-0.137) and `absmean_C` (~0.116-0.121), and all three grew proportionally together (~5-8%) over the run rather than `zs` growing disproportionately. So the earlier "`contrib_zs` dominates because the zs weight block inflated" hypothesis is closed out as false; `contrib_zs` dominance is explained by `zs` being an `avg_l1_norm`-normalized (mean abs forced to ~1) 256-dim vector versus the un-normalized, naturally-smaller-magnitude 128-dim `H`/`C` attention-backbone outputs — an input-distribution effect, not a weight-growth pathology. Added gradient-norm clipping (`torch.nn.utils.clip_grad_norm_`, which clips and returns the pre-clip raw norm in one call) to all three `TokenTD7Learner` optimizer steps in `learner.py`: encoder (`encoder_grad_clip=1.0`), actor (`actor_grad_clip=25.0`), critic (`critic_grad_clip=10.0`), replacing the old measure-only `_grad_norm` helper. Thresholds were picked from this run's own normal-phase grad-norm ranges (encoder ~0.01-0.3, actor ~4-18, critic ~0.3-6) with headroom, so normal training is unaffected but any single-batch explosion is capped before it can corrupt weights. The existing `grad/*_norm` W&B keys still report the pre-clip raw norm, so the explosion signature remains visible in logs if it recurs; no new W&B keys, no reward/action/observation/checkpoint format changes. |
| 2026-06-25 | b_rl | a[-1,1]_b[0,1] | G | False | puretokcrit | Diagnostics-only fix: `reward/rail_tat` previously logged only `_last_rail_tat_penalty`, the value for whichever rail happened to be last in the per-step loop — since `rail_tat` credit is a sparse event (most rails are 0 most steps), this almost always showed 0 and hid the real signal. Added `_rail_tat_diag()` (`ClientAlgorithm.py`) computing aggregate stats over every rail that received nonzero credit this step: `reward/rail_tat_event_count`, `reward/rail_tat_mean`, `reward/rail_tat_max`, `reward/rail_tat_sum`. Logged from both `ClientAlgorithm.py` and `ClientAlgorithm_region.py` (region inherits the helper unchanged); added the 4 new keys to `export_wandb_run.py` KEYS. No reward formula change. |
| 2026-06-25 | b_rl | a[-1,1]_b[0,1] | G | False | puretokcrit | For a clean A/B comparison against the original normalizer-based pipeline, restored `get_global_reward`'s tail to the original (pre-version-D) online `delta_normalizer` + `reward_freeze_after`(20000-step post-warmup freeze) + `±5` clip, replacing version E/F's fixed `global_reward_center`(-2.145)/no-clip approach. Everything else from versions B/C/F is kept as-is: `total_completed_jobs` accumulation (fixes the `CompletedCommandCount`-is-a-per-step-delta bug), `tat_base=2.90706*60≈174.42` (eval.py-derived, corrected from 176.5), `reward_weights.tat=9.2` (std-ratio recalibrated vs `backlog`), and the new rail-attributed `rail_tat` credit term (unaffected by this change, computed independently in `get_step_reward`/local channel, not run through `delta_normalizer`). Net effect: normalization method is held constant at the original online-adaptive scheme so this run isolates whether the reward-formula fixes alone (not the normalizer-removal experiment) help. Restored `reward_freeze_after`/`global_reward_clip` config keys; removed `global_reward_center`. No W&B key changes. |
| 2026-06-25 | b_rl | a[-1,1]_b[0,1] | F | True | puretokcrit | Added rail-attributed TAT credit instead of relying solely on the global windowed-marginal-TAT term for tail-outlier delays. Diagnosed run `2if0c86y` (version E, centering only, no clip) and found `reward/marg_tat` spiking to 350-2785 (vs normal ~150-220) at low-`dN` windows where a single long-stuck job's real TAT dominates the window average, producing `reward/global_norm` swings to -137 and `critic/q_mean` blowing up to 100+ — a credit-assignment problem (the whole system gets blamed at the moment a stuck job happens to finish), not a normalization problem. New `ClientAlgorithm.py` method `_update_rail_tat_credit(pclient)` accumulates each OHT's `PassTimes` (rail_id, time-spent) every step into `self._oht_route_accum[oht_id]`; when that OHT's `CmdCompleteTat` reports a completed command (simulator-provided per-OHT real job TAT), the relative excess over `tat_base` is distributed across the accumulated route proportional to time spent per rail, into `self._rail_tat_credit[rail_id]` (falls back to the OHT's current `RouteList[0]` rail if no accumulated route exists), then that OHT's accumulator is cleared for its next job. `get_step_reward` now subtracts `reward_weights["rail_tat"] * rail_tat_penalty` (new weight, default `1.0`, not yet empirically calibrated — watch `reward/rail_tat` and recalibrate like `tat`/`backlog` were). The existing global `tat_term`/`marg_tat`/`backlog`/`op` machinery, `global_reward_center` (`-2.145`), and weights are unchanged; this only adds a new, separately-attributed rail/region-level channel. State (`_oht_route_accum`, `_rail_tat_credit`) resets on episode `Reset()`, persists across `on_new_connection()` reconnects. Added `reward/rail_tat` to both `ClientAlgorithm.py`/`ClientAlgorithm_region.py` W&B logs and to `export_wandb_run.py` KEYS (also backfilled the previously-missing `reward/step_reward`, `global_norm`, `local_norm`, `alpha`, `marg_tat`, `backlog`, `smooth_mean` keys for the region path, which had never been added despite being logged for several prior entries in this file). |
| 2026-06-24 | b_rl | a[-1,1]_b[0,1] | E | True | puretokcrit | Added a fixed `global_reward_center` constant (`-2.145`, measured as the raw `g` mean from run `acqlbv54`, version D, steps 0-17100) to `get_global_reward`: `return raw - center`. Diagnosing version D (no normalizer, no centering) against the prior normalizer-based run `7itljki5` showed `critic/q_one_region_delta` and `critic/q_one_token_delta` (action-perturbation Q-sensitivity probes) dropped ~20x, consistent with the `backlog` term's large near-constant offset (mean raw ≈ -1.9 from `backlog` alone) dominating Q's variance and leaving little for the critic to discriminate by action. Centering removes that constant offset without reintroducing the online-adaptive (std-dividing) normalizer's replay-buffer label-drift problem, since it's a single fixed number, not updated online. Still no online normalization, no clip. `reward_weights` (`tat=9.2, op=5.0, backlog=0.01`) unchanged from version D. |
| 2026-06-24 | b_rl | a[-1,1]_b[0,1] | D | False | puretokcrit | Removed the online `delta_normalizer`/clip from `get_global_reward` (`ClientAlgorithm.py`, shared by the region harness): `g` is now the raw fixed weighted sum `w_tat*tat_term + w_op*d_op - w_backlog*backlog` directly, no z-score normalization, no centering, no `±5` clip. Rationale: with a replay buffer, an online-adapting normalizer gives the same real-world situation a different reward label depending on when the transition was pushed (label drift before `reward_freeze_after`), which is a structural inconsistency for off-policy critic training; the experiment also tests whether the normalizer's adaptive scaling was actually helping or just diluting the (now-meaningful) `tat_term` signal. Re-derived `reward_weights.tat` from `1.0` to `9.2` using real run `7itljki5` stats (`std(tat_term)≈0.0602`, `std(0.01*backlog)≈0.1113`) so the tat-term's raw contribution is ~5x the backlog term's, per request. `op` weight left unchanged (no reliable per-step `d_op` estimate available from the every-100-step W&B export). Removed now-unused `reward_freeze_after` and `global_reward_clip` config keys; `self.delta_normalizer`/`delta_normalizer_frozen` objects are left in place only for checkpoint save/load format compatibility but are otherwise inert. Local reward normalization (`local_reward_normalizer`) is unchanged. No W&B key names changed. |
| 2026-06-24 | b_rl | a[-1,1]_b[0,1] | C | False | puretokcrit | Corrected the `tat_base` baseline used in `get_global_reward`'s `tat_term = (tat_ref - tat_ema)/tat_ref` from `176.5` (a directly-measured first-10000-step average, same seconds unit as `pclient.TotalTat`) to `2.90706*60 ≈ 174.42` (the `eval.py` DB-derived baseline, converted from minutes to seconds), since the `eval.py` figure is considered the more accurate ground truth. Also fixed `Reset()`'s end-of-episode `tat_total_base`, which previously compared the seconds-unit `pclient.TotalTat` against a hardcoded `2.90706` (minutes) baseline with no unit conversion, producing a meaningless improvement percentage; it now reads `self.config["tat_base"]` (already in seconds) so both stay in sync. No other reward terms, cost mapping, or W&B keys changed.|
| 2026-06-24 | b_rl | a[-1,1]_b[0,1] | B | False | puretokcrit | Fixed a bug in `get_global_reward` (`ClientAlgorithm.py`, shared by the region harness): `CompletedCommandCount` is the number of jobs completed *this step* (delta), not a cumulative count, so multiplying it directly by `TotalTat` (a true cumulative average) and diffing against the previous step produced a meaningless `marg_tat`/EMA. Added `self.total_completed_jobs`, a running sum of the per-step delta, reset to 0 each episode `Reset()` and preserved across `on_new_connection()` reconnects; `get_global_reward` now uses this accumulated total for the `avg*count=sum` reconstruction. Also fixed `Reset()`'s end-of-episode `trcnt_total_new` log/print, which previously read the raw last-step delta instead of the episode total. No W&B key names changed, so `export_wandb_run.py` was not updated. Action selection, cost mapping, region partitioning, and checkpoint semantics are unchanged. Existing checkpoints remain weight-compatible but resumed runs restart `total_completed_jobs` at 0 (not persisted in checkpoint metadata). |
| 2026-06-24 | b_rl | a[-1,1]_b[0,1] | A | False | puretokcrit | Added outer-loop timing diagnostics to `ClientAlgorithm_region.py` W&B logs to identify the new slowdown source: `timing/setup_ms`, `timing/obs_ms`, `timing/region_cache_ms`, `timing/baseline_ms`, `timing/reward_ms`, `timing/push_ms`, `timing/learn_ms`, `timing/action_ms`, `timing/set_cost_ms`, `timing/log_ms`, `timing/log_diag_ms`, and `timing/step_total_ms`. The timing accumulator resets when warmup ends so learning-phase bottlenecks are not diluted by 10k warmup steps. These complement existing learner internals (`learner/collate_ms`, `learner/critic_forward_ms`, `learner/critic_backward_ms`, `learner/actor_forward_ms`). Reward formula, replay reward aggregation, cost mapping, model structure, action curriculum, batch size, training flow, and checkpoint semantics are unchanged. Updated `export_wandb_run.py`. |
| 2026-06-24 | b_rl | a[-1,1]_b[0,1] | A | False | puretokcrit | Added active-vs-inactive region action diagnostics using `parameterC`: a region is active when any rail in it has `c>0`, otherwise inactive. New keys include active/inactive region counts, active ratio, action mean/std/abs_mean, and active/inactive in-region action std/range summaries. This separates meaningful action variation from regions where `c=0` makes `cost=base+w*c*b_rl` insensitive to action. No action masking/clamping was added; reward formula, replay reward aggregation, cost mapping, model structure, training flow, and checkpoint semantics are unchanged. Updated `export_wandb_run.py`. |
| 2026-06-24 | b_rl | a[-1,1]_b[0,1] | A | False | puretokcrit | Added region action dispersion diagnostics to `ClientAlgorithm_region.py`: `in_region_per_rail_action_std/{mean,max,min}` summarizes how different rail actions are inside each region across all regions, and region comparison keys (`region_action/region_mean_{min,max,range}`, `region_action/in_region_range_{mean,max,min}`) expose cross-region action spread without sampling a random region. Reward formula, replay reward aggregation, cost mapping, model structure, training flow, and checkpoint semantics are unchanged. Updated `export_wandb_run.py` for the new metric keys. |
| 2026-06-24 | b_rl | a[-1,1]_b[0,1] | A | False | puretokcrit | Aligned `ClientAlgorithm_region.py` W&B logging closer to the per-rail `ClientAlgorithm.py` baseline. Removed warmup-phase region logging, stopped logging duplicate `system/*` KPI keys in the region harness, added `global/queued_jobs` alongside the existing `global/queued` alias, and made `reward/step_reward` report the last per-rail step reward as in the per-rail harness while keeping `region_reward/*` as additional region diagnostics. Reward formula, region reward aggregation used for replay, cost mapping, model structure, and training flow are unchanged. Updated `export_wandb_run.py` for `global/queued_jobs`. |
| 2026-06-23 | b_rl | a[-1,1]_b[0,1] | A | False | puretokcrit | Changed the region-token TD7 critic from the hybrid `Q_region(zs,zsa)+sqrt(N)*mean(token_adv)` form to a pure token aggregate critic: `Q=sum_i q_i/sqrt(N_valid)`, where each token Q sees `[H_i,C,zs,zsa,a_i]`. Removed the region value branch from `RegionTD7Critic` and added token-Q diagnostics (`critic/q_token_*`, `critic/q_agg_*`, `critic/q_valid_tokens_mean`) routed through learner stats/export. Encoder loss, reward formula, cost mapping, region partitioning, actor output shape/range, warmup, target smoothing, and fixed encoder update order are unchanged. Fresh region TD7 checkpoints are required because critic parameter shapes changed. |
| 2026-06-23 | b_rl | a[-1,1]_b[0,1] | A | False | regioncrit | Changed only the region-token TD7 critic structure. `RegionTD7Critic` now computes twin Q as `Q_region(zs,zsa) + sqrt(N_valid)*mean_i(Q_token(H_i,C,zs,zsa,a_i))`, so rail-token action contributions are visible to the critic instead of being compressed to mean/max action summary. Learner critic calls now pass `H, C, zs, zsa, action, mask` from the same fixed encoder snapshot. Encoder loss, reward formula, cost mapping, region partitioning, actor output shape/range, warmup, target smoothing, and fixed encoder update order are unchanged. |
| 2026-06-23 | b_rl | a[-1,1]_b[0,1] | A | False | regiontd7 | Added a separate `PythonCode/ClientAlgorithm_region.py` region-token TD7 harness selected by `python PythonCode/main.py --region`. Region TD7 code lives under `PythonCode/cocel_rl/algorithms/token_td7/` with concat-conditioned rail-token attention, variable-size region replay padding masks, and TD7-style encoder/critic/actor updates. The original `ClientAlgorithm.py` per-rail path and existing `cocel_rl` TD3/TD7 internals are unchanged. Cost mapping remains `cost=base+w*c*b_rl`, `b_rl=0.5+0.5*a`; the existing per-rail reward formula is preserved and aggregated to region reward by mean. Added region action/reward/mask/Q diagnostics and updated `export_wandb_run.py`. Fresh checkpoints are required because model, replay, and transition formats are incompatible with per-rail TD7 checkpoints. |
| 2026-06-23 | base_exp_multiplier | rail_token_a[-1,1]->base*exp(0.3*a) | direct_v1_early_queue_terminal | False | base_mult_a03_trace_oht10_unclipped_noise0 | Removed behavior-action EMA smoothing and set `algorithm.noise_scale=0.0` for a deterministic behavior-action comparison run after random exploration. The applied action is now the finite-guarded, clipped raw actor/random action; it is converted back into per-region actions before `last_region_actions` is stored, so replay buffer actions still match `mult=exp(alpha*a)` cost assignment. Target policy smoothing remains unchanged at `target_noise_scale=0.2`. Reward formula, base exponential cost mapping, observation shape, action range, timestamped OHT trace CSV, W&B metrics, Dijkstra/Assign, learner/replay APIs, and no-buffer checkpoint behavior are unchanged. |
| 2026-06-23 | base_exp_multiplier | rail_token_a[-1,1]->base*exp(0.3*a) | direct_v1_early_queue_terminal | False | base_mult_a03_trace_oht10_unclipped_ema09 | Changed the fixed-OHT CSV trace output path to be timestamped once per process run. The config `oht_route_trace.path` now acts as the base filename, and the actual file becomes e.g. `debug_trace_oht_10_YYYYMMDD_HHMMSS.csv`; startup prints the resolved trace path. This prevents separate runs from appending into the same trace CSV. Reward formula, action EMA/cost mapping, observation shape, action range, W&B metrics, Dijkstra/Assign, learner/replay APIs, and checkpoint behavior are unchanged. |
| 2026-06-23 | base_exp_multiplier | rail_token_a[-1,1]->base*exp(0.3*a) | direct_v1_early_queue_terminal | False | base_mult_a03_trace_oht10_unclipped_ema09 | Added behavior-action EMA smoothing for the base exponential multiplier run with `direct_action.action_ema_beta=0.9`. The raw actor/random action is filtered first, then clipped, converted back into per-region actions, stored in `last_region_actions`, and used for `mult=exp(alpha*a)` cost assignment, so replay buffer actions match the simulator-applied actions. EMA state resets on `Reset()` and terminal `done` cleanup. Reward formula, observation shape, action range, OHT trace diagnostics, Dijkstra/Assign, learner/replay APIs, and no-buffer checkpoint behavior are unchanged. No `export_wandb_run.py` update was needed because no W&B metric keys were added or renamed. |
| 2026-06-23 | base_exp_multiplier | rail_token_a[-1,1]->base*exp(0.3*a) | direct_v1_early_queue_terminal | False | base_mult_a03_trace_oht10_unclipped | Made the unclipped reward setting explicit in config with `reward.use_reward_clip=False` and updated `EXP_META.note` so the W&B run name records that this run is unclipped. Fixed the startup print from `direct_v0` to `direct_v1 global delta_tat + early_queue + reassign + terminal` to match the actual reward config. Reward formula, queue/reassign/terminal weights, base exponential cost mapping, observation shape, action range, OHT trace diagnostics, Dijkstra/Assign, learner/replay APIs, and no-buffer checkpoint behavior are unchanged. No `export_wandb_run.py` update was needed because no W&B metric keys were added or renamed. |
| 2026-06-23 | base_exp_multiplier | rail_token_a[-1,1]->base*exp(0.3*a) | direct_v1_early_queue_terminal | False | base_mult_a03_trace_oht10 | Changed direct cost assignment from absolute normalized cost `max(0.5*(a+1), cost_floor)` to a base-cost exponential multiplier: `mult=exp(0.3*a)` and `FRailLineCost=max(1e-6, base*mult)`. Added `direct_action.mode='base_exp_multiplier'`, `mult_alpha=0.3`, and `cost_floor=1e-6`; `_last_mult_arr` now stores the actual multiplier. This restores base travel-time scale while keeping the same action range, reward formula, observation shape, warmup behavior, OHT trace diagnostics, Dijkstra/Assign, learner/replay APIs, and no-buffer checkpoint behavior. Existing direct-action checkpoints are intentionally incompatible through `EXP_META.cost_structure/action_range`. |
| 2026-06-23 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v1_early_queue_terminal | False | qref50_qdelta05_reassign10_termfix | Extended OHT route diagnostics into a three-layer setup. Fixed-OHT CSV trajectory tracing remains per-step for 10 active-first OHTs. W&B scalar route/job summaries are logged every 100 steps and at terminal steps (`route_diag/route_len_mean`, base/final route sum mean, route cost ratio mean, `oht_state/*`, and `job_pipeline/*`). Terminal steps optionally log a capped active-first OHT table under `route_diag/terminal_oht_table` for final queue-failure inspection. Added `wandb_summary_every`, `terminal_table_enabled`, and `terminal_table_max_oht` to `oht_route_trace`, and updated `export_wandb_run.py` for the new scalar keys. Reward formula, direct cost mapping, observation shape, action range, warmup behavior, Dijkstra/Assign, learner/replay APIs, and checkpoint compatibility behavior are unchanged. |
| 2026-06-23 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v1_early_queue_terminal | False | qref50_qdelta05_reassign10_termfix | Replaced the W&B route table diagnostics with a config-controlled fixed-OHT CSV trace. Added `use_oht_route_trace=True` plus `oht_route_trace` config (`max_oht=10`, `every=1`, `path=./debug_trace_oht_10.csv`, `route_head_len=20`). At the first post-warmup trace point, active OHTs are chosen first and the fixed IDs are reused; each post-cost step appends per-OHT route/current-line/job/global fields including remaining-route base/final cost sums. Terminal steps log using the last applied action/cost arrays before episode state is cleared. Removed the just-added `route_diag/*` and `job_pipeline/*` W&B export keys. Reward formula, direct cost mapping, observation shape, action range, warmup behavior, Dijkstra/Assign, learner/replay APIs, and checkpoint compatibility behavior are unchanged. |
| 2026-06-23 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v1_early_queue_terminal | False | qref50_qdelta05_reassign10_termfix | Added W&B route-level diagnostics instead of local CSV files. Every 100 steps, and always on terminal steps, `ClientAlgorithm_Direct.py` now logs a capped OHT route table (`route_diag/oht_routes`, active OHT first, max 200 rows) with per-OHT route length, route base/final cost sums, route cost ratio, route density, route parameterC pressure, current-line cost/action/density/region, and job endpoints. It also logs route summary scalars under `route_diag/*` and job-state pipeline counts under `job_pipeline/*`; `export_wandb_run.py` now includes those scalar keys and tail summaries. Reward formula, direct cost mapping, observation shape, action range, warmup behavior, Dijkstra/Assign, learner/replay APIs, and checkpoint behavior are unchanged. |
| 2026-06-22 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v1_early_queue_terminal | False | qref50_qdelta05_reassign10_termfix | Retuned the direct global reward for earlier queue pressure and clearer terminal accounting. Set `queue_ref=50.0`, `reassign_scale=10.0`, weights to `w_delta_tat=0.40`, `w_queue=0.45`, `w_reassign=0.15`, terminal fail penalty to `3.0`, and reward clip bounds metadata to `[-5, 1]` while leaving clipping disabled by default. Changed queue reward from `0.7*queue_level^2 + 0.3*queue_delta` to an even `0.5/0.5` mix. Kept terminal-step W&B logging forced with `or done` and the `[TERMINAL REWARD]` console print. Direct cost mapping, observation shape, action range, warmup behavior, Dijkstra/Assign, learner/replay APIs, and no-buffer checkpoint behavior are unchanged. |
| 2026-06-22 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v1_delta_tat_queue_reassign_terminal_unclipped | False | direct_ckpt_nobuf | Changed direct checkpoint policy to not save or resume the replay buffer. Checkpoints still save actor/critic weights, optimizer states, learner step counter, token normalizers, direct/reward/algorithm/region config metadata, and baseline parameter caches; metadata records `buffer_saved=False` and `buffer_size_at_save` for traceability only. Resume now ignores any legacy checkpoint buffer if present and restarts with a fresh replay buffer. Reward formula, direct cost mapping, observation shape, action range, Dijkstra/Assign, warmup formula, and learner/replay APIs are unchanged. |
| 2026-06-22 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v1_delta_tat_queue_reassign_terminal_unclipped | False | direct_ckpt | Enabled checkpoint saving by default for `ClientAlgorithm_Direct.py` (`save_model=True`) and fixed direct-token checkpoint contents/resume. Saved checkpoints now include the token replay buffer, learner update step counter, token normalizers, direct/reward/algorithm/region config metadata, buffer size, and baseline parameter caches. Resume now verifies algorithm, obs/action dims, target region size, token model config, and `EXP_META` cost/action/reward compatibility before loading; incompatible or missing checkpoints no longer skip warmup. Successful resume restores actor/critic/optimizers, target networks, replay buffer, learner step counter, rail/global/region normalizers, and parameter caches. Reward formula, direct cost mapping, observation shape, action range, Dijkstra/Assign, warmup formula, and learner/replay APIs are unchanged. |
| 2026-06-22 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v1_delta_tat_queue_reassign_terminal_unclipped | False | direct_unclip | Made direct-action reward clipping optional and disabled by default via `reward.use_reward_clip=False`. Replay now receives unclipped `r_global_raw` unless the toggle is explicitly enabled; `reward_clip_low/high` remain available as a manual safety switch. Added `reward/direct/use_clip` and `reward/direct/clip_applied` diagnostics and updated `export_wandb_run.py` for the unclipped reward keys. Cost mapping, observation shape, action range, warmup/random exploration, learner/replay APIs, Dijkstra/Assign, and checkpoint resume behavior are unchanged. |
| 2026-06-22 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v0_delta_tat_queue_reassign_terminal | False | direct_noise | Fixed direct-action batched actor exploration to read `algorithm.noise_scale` from config instead of optional `actor.noise_scale`, so post-random-exploration behavior actions keep TD3 exploration noise in the vectorized actor path. Reward formula, observation shape, replay/learner APIs, random warmup length, direct cost mapping, and checkpoint resume setting are unchanged. Clarified legacy cost config as logging compatibility only, made GUI `reward` reflect direct global reward while preserving `density_reward`, added direct-cost Q-probe aliases (`q_cost_low/mid/high/range`), and aligned `export_wandb_run.py` with the direct W&B project and new Q-probe keys. |
| 2026-06-22 | direct_action_cost | rail_token_a[-1,1]->cost[0.01,1.0] | direct_v0_delta_tat_queue_reassign_terminal | False | direct_action_v0 | Added separate `PythonCode/ClientAlgorithm_Direct.py` direct-action region-token TokenTD3 harness. It preserves variable-size region tokens, replay/learner APIs, observation features, region partitioning, warmup/random exploration structure, Dijkstra/Assign/UpdateOHTRoute logic, and checkpoint resume disabled. After warmup, actor output maps directly to Dijkstra cost with `cost=max(0.5*(a+1), 0.01)`; `base`, `parameterDw`, `parameterC`, and `w*c` remain observation/diagnostic inputs only. Warmup uses baseline `base+w*c*0.5`. Replay reward changes to one shared global direct reward per region transition: delta-TAT, queue level/delta penalty, reassignment-delta penalty, and terminal failure penalty clipped to `[-3, 1]`. Added direct reward/cost diagnostics and `--algo region-direct-cost` in `PythonCode/main.py`; updated `export_wandb_run.py` for direct metrics. |
| 2026-06-22 | fixed_policy_wc_cap | b_policy | baseline | False | baseline_cli | Added explicit baseline CLI controls in `PythonCode/main.py`: `--baseline-mode`/`--baseline_mode`, `--baseline-fixed-b`, `--baseline-base-b`, `--baseline-high-b`, `--baseline-top-percentile`, and `--baseline-risk-percentile`. CLI args now override optional `OHT_BASELINE_*` environment fallbacks in `ClientAlgorithm_baseline.py`. Fixed-policy formulas, cost mapping, W&B metrics, and main RL code are unchanged. |
| 2026-06-22 | fixed_policy_wc_cap | b_policy | baseline | False | fixed_policy | Extended the separate baseline harness in `PythonCode/ClientAlgorithm_baseline.py` from scalar fixed-b sweep to three fixed-policy comparison modes: `uniform_b`, `rule3_top_paramc`, and `rule2_wc_base_risk`. The cost formula remains capped additive `cost=base+min(w*c, cap)*b_arr`; rule3 assigns high-b to top `parameterC` rails, and rule2 assigns continuous b from capped `wc_eff/base` risk. Added fixed-policy/b-distribution/rule diagnostics plus episode queue/TAT/op-rate accumulators, aligned baseline W&B logging with the `oht-routing-baseline-b-sweep` export project, and updated `export_wandb_run.py` for the new baseline keys. Main RL learner, replay buffer, reward learning logic, observation shape, Dijkstra, Assign, and `PythonCode/ClientAlgorithm.py` are unchanged. |
| 2026-06-22 | fixed_policy_wc_cap | b_policy | baseline | False | fixed_param_reset | Reset baseline cost-state caches (`parameterDw`, `parameterPassTimes`, and `parameterC`) at each `ClientAlgorithm_baseline.Reset()` so fixed-policy/b-sweep comparisons do not carry EMA route-cost state across simulator episodes. Fixed policy b config/sweep selection, capped additive cost formula, W&B keys, and main RL code are unchanged. |
| 2026-06-21 | fixed_b_wc_cap | b_fixed_sweep | baseline | False | fixed_b_sweep | Added a separate fixed-b baseline harness. New `PythonCode/ClientAlgorithm_baseline.py` applies `cost=base+min(w*c, cap)*b_fixed` with no RL learner, replay buffer, actor/critic, token observation build, GUI, or warmup. `PythonCode/main.py` now supports `--baseline --b ...`; multiple b values are used one per simulator episode through `Reset()`. W&B records the b sweep config plus per-b KPI, termination, timing, and cost/cap diagnostics. `export_wandb_run.py` includes the new baseline keys. The main RL `ClientAlgorithm.py` reward, observation shape, action range, learner, and cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | b64_l4_d2_buf100k | Reverted token learner throughput to the lighter stability setting for the next run. Set `batch_size=64`, kept `learn_every=4` and `max_rollout=1`, kept TD3 `policy_update_delay=2`, and returned replay `buffer_capacity` to 100,000. Reward, observation shape, action range, replay format, vectorized learner implementation, diagnostics, and capped additive cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | b256_l4_d8_buf500k | Increased token learner capacity for the next stability check after `b128_obsprof` showed revived policy variation but queue termination. Set `batch_size=256`, kept `learn_every=4` and `max_rollout=1`, delayed TD3 actor updates with `policy_update_delay=8`, and increased replay `buffer_capacity` to 500,000. Reward, observation shape, action range, replay format, vectorized learner implementation, diagnostics, and capped additive cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | b128_obsprof | Prepared the first vectorized learner training run after equivalence validation. Increased token learner `batch_size` from 64 to 128 while keeping `learn_every=4`, `max_rollout=1`, and `policy_update_delay=2`. Set `debug_batch_equivalence=False` for the actual run and kept the fine-grained observation-build timing diagnostics from `veclearn_equiv_obsprof`. Reward, observation shape, action range, replay format, learner vectorization, and capped cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | veclearn_equiv_obsprof | Added fine-grained observation-build timing diagnostics for the vectorized learner validation run. `timing/obs_ms` is now accompanied by raw observation subparts (`obs_raw_cache`, `obs_raw_global`, `obs_raw_rail_loop`, `obs_raw_arrays`, `obs_raw_neighbor`, `obs_graph_aug`, `obs_raw_concat`) and token-stage subparts (`obs_region_cache`, `obs_rail_token`, `obs_region_token`, `obs_normalize`). Warmup timing accumulators are reset together with the existing main timing reset, and `export_wandb_run.py` includes the new keys. Reward, observation shape, action range, replay format, learner behavior, and cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | veclearn_equiv | First validation run for vectorized token DeepSets learner. Enabled `debug_batch_equivalence=True` so learner performs one single-region and sampled-batch loop-vs-vectorized equivalence check before normal training. Also changed masked max pooling sentinels in actor/critic batch paths from `-1e9` to `torch.finfo(dtype).min` with `torch.nan_to_num(...)` safety. Reward, observation shape, action range, replay push format, and cost mapping are unchanged. After seeing `[BATCH EQUIV] token DeepSets forward_batch checks passed`, set `debug_batch_equivalence=False` and return note to `veclearn` for the actual run. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | veclearn | Vectorized the variable-size token DeepSets learner. Added mask-aware `forward_batch(...)` paths: actor returns action `[B,L,1]`, critic returns q1/q2 `[B,1]`; learner now collates sampled region transitions into padded `[B,L,*]` tensors with asserts for matching rail/action lengths and non-empty regions. Target policy smoothing noise is masked before use, critic pooling masks after token embedding to avoid padded-token bias, gradient clipping/target update/step counters/loss logging are preserved, and learner timing breakdown metrics are routed through existing W&B logging/export. Added optional `debug_batch_equivalence=False` checks for single-region and sampled-batch loop-vs-vectorized equivalence. Reward, observation shape, action range, replay push format, and cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | batched_action | Replaced per-region actor action calls in `ClientAlgorithm.py` with a batched DeepSets actor path using padded region tensors and masks. This preserves per-region mean/max pooling semantics but reduces Python calls and tensor transfers for both behavior action selection and deterministic eval-action diagnostics. Falls back to the old per-region loop if actor internals are unavailable. Reward, observation shape, action range, learner API, replay API, and cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | actionvar_eval | Cleaned up action/cost diagnostics. Removed duplicated cost scalar keys from the main dashboard so cost cap metrics are logged only after the newly computed `FRailLineCost`. Added deterministic eval-action history and explicit behavior-vs-eval action variability/correlation metrics, including eval per-rail temporal std. Added a separate `debug/top_final_cost_table` alongside `debug/top_cost_ratio_table`, and reduced cost debug plot frequency from 100 to 500 steps. Kept `rail/wc` as raw `w*c` for this run; reward, observation shape, learner, action range, and cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | actionvar | Added rolling rail-action diagnostics to distinguish static rail maps from state-dependent token actions. Every 10 steps, stores rail action, b, and density snapshots over a 200-sample window; every learning diagnostic step, logs cross-rail std, per-rail temporal std, within-region std, action-feature correlations against density/parameterC/raw wc/OHT count, and delta action vs delta density correlation. Updated `export_wandb_run.py`. Reward, observation shape, learner, action range, and cost mapping are unchanged. |
| 2026-06-21 | b_token_deepsets_wc_cap | rail_token_a[-1,1]->b[0,1] | C | False | wc_cap | Added a cap on the additive `w*c` cost term before assigning `FRailLineCost`: `wc_cap=max(base*4.0, 2.0)`, `wc_eff=min(w*c, wc_cap)`, and `cost=base+wc_eff*b_rl`. Raw `w*c` is still stored for diagnostics. Added cost/OHT scalar metrics, route-active cost metrics, 100-step W&B debug plots/tables, and updated `export_wandb_run.py`. Reward formula, token DeepSets learner, observation shape, and action range are unchanged, so reward_version remains C. |
| 2026-06-20 | b_token_deepsets | rail_token_a[-1,1]->b[0,1] | C | False | variable_region_token_deepsets | Further reduced token learner load after deciding observation cost should stay unchanged. Lowered token `batch_size` from 128 to 64, increased `learn_every` from 2 to 4, and reduced Q-probe sample count from 32 to 16. With `max_rollout=1`, this makes average learner sample work roughly 1/64 of the original `max_rollout=4`, `batch_size=256`, every-step setup. Reward formula unchanged, so reward_version remains C. |
| 2026-06-20 | b_token_deepsets | rail_token_a[-1,1]->b[0,1] | C | False | variable_region_token_deepsets | Speed tuning after simulator timing showed `learn` around 2100ms/step and `obs` around 350ms/step. Reduced token `batch_size` from 256 to 128, `max_rollout` from 4 to 1, added `learn_every=2`, changed TD3 `policy_update_delay` from 8 to 2 for the new rollout cadence, and reduced Q-probe sample count from 128 to 32 to avoid large 500-step diagnostic spikes. Optimized per-step graph augmentation by keeping the legacy 13-column layout but computing only token-consumed fields: load, relative load, downstream/upstream 1-hop mean, degree, and active flag. Reward formula unchanged, so reward_version remains C. |
| 2026-06-20 | b_token_deepsets | rail_token_a[-1,1]->b[0,1] | C | False | variable_region_token_deepsets | Trimmed W&B logging for the token DeepSets run. Kept a lightweight 100-step dashboard (`phase/random`, buffer, KPI, reward, density, token action/b, cost, termination) and a focused 500-step learning diagnostic set (loss, eval token action/b, Q-probe, grad, obs clip, region partition quality, timing). Removed histogram, buffer_sample, duplicate action/region_action/raw/applied/brl/rail_expanded, priority, numeric/model, reward/raw/nonzero/active diag, and density/balance duplicate W&B logs. Added region graph quality and timing keys to `export_wandb_run.py`. Reward formula unchanged, so reward_version remains C. |
| 2026-06-20 | b_token_deepsets | rail_token_a[-1,1]->b[0,1] | C | False | variable_region_token_deepsets | Safety tuning for first token DeepSets run. Lowered token replay `batch_size` from 4096 to 256, reduced `buffer_capacity` from 1,000,000 to 100,000, and set `save_model=False` for initial simulator testing. Variable BFS regions now call `_merge_small_regions(groups, min_size=max(10, target_size // 3))` before region cache creation. Added partition quality metrics: `region/size_std`, `region/internal_density_std_mean`, `region/internal_density_std_max`, and `region/internal_density_mean_std`; export parser updated. Reward formula unchanged, so reward_version remains C. |
| 2026-06-20 | b_token_deepsets | rail_token_a[-1,1]->b[0,1] | C | False | variable_region_token_deepsets | Replaced the fixed `K=50` padded region-block MLP path with variable-size graph-BFS region token TD3. Regions now use `_region_members: list[np.ndarray]` with target size 30 and no padding; each replay item stores one region transition with `rail_feat [n_r,20]`, `context_feat [24]`, token actions `[n_r,1]`, scalar region reward, next token features, and done. Added token DeepSets actor/critic, token replay buffer, and token learner without changing the existing MLP TD3/Learner/ReplayBuffer APIs. Cost mapping remains `cost = base + parameterDw * parameterC * b_i`, `b_i=0.5*(a_i+1)`. Reward remains version C semantics but is now variable-region token aggregation: clipped rail exponential congestion reward, then `0.6*mean + 0.4*bottom10_mean`. Added token_action/token_b logs and one-token Q-probe diagnostics; `resume=False` remains required because model, obs, action, and buffer formats are incompatible with all previous checkpoints. |
| 2026-06-19 | b_region_block | a[-1,1]->b_slot[0,1] | C | False | region_block_k50_rail_reward_sum | Converted the region experiment to region-block joint-action RL. Each region transition now stores a 50-slot action vector (`action.shape == [num_regions, 50]`), padded region blocks use `rail_idx=-1` and mask 0, and actor/critic `act_dim=50`. Observation is region block format: 10 global + 24 region aggregate + 50 compact rail feature slots + 50 mask values. Reward is computed per rail with exponential congestion, clipped to `[-reward_clip, 0]`, then aggregated as `sum(rail_reward in region) / K`; p90/top-k density reward is no longer used as the learned reward. Cost mapping remains `cost = base + parameterDw * parameterC * b_rail`, where `b_rail` comes from the rail's region local slot. Added all-action and one-slot Q-probe diagnostics plus region_b, rail-expanded action, rail_reward, and rail_density logging/export keys. Fresh run required; `resume=False` remains set. |
| 2026-06-19 | b_rl | a[-1,1]->b[0,1] | D | False | cstable | Reward C-stable implementation on top of the region-action experiment. Replaced region p90 density reward with top-5 mean rail density per region: `reward_scale * (exp(-top5_mean_density / density_scale) - 1)`. Kept queue penalty out of the per-step reward to preserve region credit assignment. Added weak region action regularization in replay reward, `-action_reg_scale * action^2`, with `action_reg_scale=0.001`. Terminal failure penalty remains `1.0`; `resume=False` remains required. |
| 2026-06-19 | b_rl | a[-1,1]->b[0,1] | C | False | region50 | Switched `PythonCode/ClientAlgorithm.py` from per-rail transitions to graph-BFS region-action RL. Rails are grouped by undirected rail graph connectivity from `DivergingLineIDList` with target size 50 and min size 20. Actor/critic observations, actions, rewards, and replay pushes are now region-level; rail costs receive broadcast region `b_rl` using `cost = base + parameterDw * parameterC * b_region`. Reward changed to pure region p90 density congestion `reward_scale * (exp(-region_density / density_scale) - 1)`, with optional terminal failure penalty only. Added region reward/density/action diagnostics and export keys. Fresh run required; `resume=False` remains set because obs/action batch semantics changed. |
| 2026-06-19 | b_rl | a[-1,1]->b[0,1] | B | False | exp_congestion_s003 | Replaced the reward with pure per-rail exponential congestion: `r_raw = exp(-density / cong_scale) - 1`, clipped to `[-reward_clip, 0]`. Removed delta imbalance/load, queue, global delta, route boost, inactive scaling, and action regularization from the learned reward while keeping terminal failure penalty, queue_penalty, and previous-state updates for compatibility/logging. Added `cong_scale=0.03` and density distribution logs/export keys. Fresh run required; `resume=False` remains set. |
| 2026-06-18 | b_rl | a[-1,1]->b[0,1] | A | False | rand300 | Set `EXP_META.note='rand300'` and increased `random_exploration_steps` from 100 to 300 for the TD3 random action coverage experiment. Added `phase/random_exploration_steps` and `phase/random_exploration_end_step` to wandb/export so the random phase boundary is explicit. Synchronized `PythonCode/ClientAlgorithm copy 2.py` with `PythonCode/ClientAlgorithm.py` to avoid running the stale SAC/direct copy by accident. Reward formula unchanged in the main experiment, so `reward_version` remains A. |
| 2026-06-18 | b_rl | a[-1,1]->b[0,1] | A | False | random_warmup | Added random action exploration after normalization warmup. `random_exploration_steps=100`; actor is ignored during this phase and uniform `[-1,1]` actions are stored in replay. Learning is delayed until `warmup_steps + random_exploration_steps` so first critic updates see random action coverage. Added `phase/is_random_exploration` to wandb and `export_wandb_run.py`. Reward formula unchanged, so `reward_version` remains A. |

실험 변경 이력. `reward_version` 또는 코드 구조(action 방식 등)를 바꿀 때마다 한 줄씩 추가한다.
EXP_META는 `PythonCode/ClientAlgorithm.py` 상단에 정의되어 있으며 wandb run name/config에 자동 반영됨.

| 날짜 | cost_structure | action_range | reward_version | centering | note | 변경 내용 |
|------|------|------|------|------|------|------|
| 2026-06-18 | residual | (1-x,1+x) | A | True | residual_baseline | 기존 운영 버전: cost = baseline(base+0.5wc) * clip(1+residual_scale*applied_a). applied_a는 raw action에서 전체 rail 평균(global 성분)을 뺀 값 (mean-centering). |
| 2026-06-18 | b_rl | a[-1,1]->b[0,1] | A | False | diag | residual multiplier → additive b_rl cost로 전환 (진단용, 최종 기법 아님). cost = base + w*c*b_rl, b_rl=0.5*(a+1). mean-centering 제거 (uniform shift도 rail별 wc가 달라 cost에 영향을 주므로). reward는 변경하지 않음(reward_version 그대로 A). 추가 진단: reward-action correlation(상태 버킷별), cost sensitivity(b=0 vs b=1), eval b diversity, critic_probe first_step 메타데이터, brl/* 비용·행동 통계. export_wandb_run.py KEYS에 신규 키 반영. |
| 2026-06-18 | b_rl | a[-1,1]->b[0,1] | A | False | obs_clip_diag | reward/cost/b_rl 매핑/warmup/actor·critic/기존 진단 변경 없음. 정규화된 obs(z-score)에 `[-5,5]` clip 추가 (StateNormalizer.normalize 및 get_local_observations의 is_fixed 경로 양쪽). clip 효과 측정을 위해 clip 전(z_preclip) 분포 기준으로 `obs/clip_ratio`, `obs/abs_max`, `obs/abs_p99` 로깅. 가설: heavy-tail per-rail feature(param_dw, jobs_from/to, nbr_reservation 등)가 actor tanh를 saturate시킨다는 의심을 obs/clip_ratio로 반증 가능하게 함. export_wandb_run.py KEYS에 `obs/*` 3개 추가. |
